import {ReactComponent as UserSvg} from "../../assests/icons/Icons - Members.svg"

export const AddUserIcon = (props) => {
    return (<UserSvg width={props.width} height={props.height} className={props.className}/>)
}