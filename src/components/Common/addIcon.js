import {ReactComponent as AddSvg} from "../../assests/icons/Icons - Add.svg"

export const AddIcon = (props) => {
    return <AddSvg  width={props.width} height={props.height} className={props.className} fill={props.fill}/>
}