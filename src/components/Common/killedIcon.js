import {ReactComponent as KilledSvg} from "../../assests/icons/Icons - Killed.svg"

export const KilledIcon = (props) => {
    return(<KilledSvg width={props.width} height={props.height} className={props.className}/>)
}