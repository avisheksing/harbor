import {ReactComponent as FailedSvg} from "../../assests/icons/Icons - Attention.svg"

export const FailedIcon = (props) => {
    return <FailedSvg height={props.height} width={props.width} className={props.className}/>
}