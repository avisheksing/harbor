import {ReactComponent as DropdownSvg} from "../../assests/icons/Icons - Arrow-Down.svg"

export const DropDownIcon = (props) => {
    return (<DropdownSvg width={props.width} height={props.height} className={props.className}/>)
}