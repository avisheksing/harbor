import {ReactComponent as KeySvg} from "../../assests/icons/Icons - User key.svg"

export const KeyIcon = (props) => {
    return(
        <KeySvg width={props.width} height={props.height} className={props.className}/>
    )
}