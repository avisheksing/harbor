import {ReactComponent as BackSvg} from "../../assests/icons/Icons - Left arrow.svg"

export const BackIcon = (props) => {
    return(<BackSvg width={props.width} height={props.height} className={props.className}/>)
}