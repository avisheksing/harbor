import {ReactComponent as TimeSvg} from "../../assests/icons/Icons - Time.svg"

export const TimeIcon = (props) => {
    return(
        <TimeSvg width={props.width} height={props.height} className={props.className}/>
    )
}