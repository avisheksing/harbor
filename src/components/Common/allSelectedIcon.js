import {ReactComponent as AllSelectedSvg} from "../../assests/icons/Icons - All Status.svg"

export const AllSelectedIcon = (props) => {
    return( <AllSelectedSvg width={props.width} height={props.height} className={props.className}/> )
}