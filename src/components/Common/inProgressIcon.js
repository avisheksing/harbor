import {ReactComponent as InProgressSvg} from "../../assests/icons/Icons - Standing up.svg"

export const InProgressIcon = (props) => {
    return (
        <InProgressSvg width={props.width} height={props.height} className={props.className}/>
    )
}