import {ReactComponent as FiddleSvg} from "../../assests/icons/Icons - Testnets.svg"

export const FiddleIcon = (props) => {
    return(<FiddleSvg width={props.width} height={props.height} className={props.className}/>)
}