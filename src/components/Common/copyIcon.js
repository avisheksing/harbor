import {ReactComponent as CopySvg} from "../../assests/icons/Icons - Clone.svg"

export const CopyIcon = (props) => {
    return( <CopySvg width={props.width} height={props.height} className={props.className}/>)
}