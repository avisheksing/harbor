import {ReactComponent as CircleSvg} from "../../assests/icons/Icons - Tick-in-circle.svg"

export const CircleTickIcon = (props) => {
    return (<CircleSvg width={props.width} height={props.height} className={props.className}/>)
}