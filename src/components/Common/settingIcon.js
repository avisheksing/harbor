import {ReactComponent as SettingSvg} from "../../assests/icons/Icons - Settings.svg"

export const SettingIcon = (props) => {
    return(
        <SettingSvg width={props.width} height={props.height} className={props.className}/>
    )
}