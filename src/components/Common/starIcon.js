import {ReactComponent as StarSvg} from "../../assests/icons/Icons - Project.svg"

export  const StarIcon = (props) => {
    return <StarSvg width={props.width} height={props.height} className={props.className}/>
}