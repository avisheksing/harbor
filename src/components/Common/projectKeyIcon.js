import {ReactComponent as ProjectSvg} from "../../assests/icons/Icons - Project key.svg"

export const ProjectKey = (props) => {
    return (<ProjectSvg width={props.width} height={props.height} className={props.className}/>)
}